---
name: 50.0-managed-identity-bootstrap
overview: Bootstrap server-side Miso application tokens and permitted runtime secrets through the Miso Entra-authenticated broker without preexisting Miso credentials or Key Vault access.
status: approved
approvedBy: user
approvedDate: 2026-09-19
planValidatedStatus: validated
planValidatedDate: 2026-09-19
createdBy: Mika Roivainen
planType: code
surfaces:
  - backend
  - tests
  - docs
isProject: false
todos:
  - id: before-development
    content: Review fixed v1 policy, prerequisite fixture delivery and baseline gates before product edits.
    status: pending
  - id: shared-contract
    content: Align typed bootstrap request and response with controller 227.4 and sibling SDK.
    status: pending
  - id: local-provider-packaging
    content: Implement frozen mode selection, local runtime parity and the optional Azure extra.
    status: pending
  - id: pre-client-bootstrap
    content: Obtain an Entra token and call the broker before constructing token-only MisoClientConfig.
    status: pending
  - id: token-provider-integration
    content: Add token-only configuration and integrate broker renewal into normal SDK authentication.
    status: pending
  - id: snapshot-lifecycle
    content: Own runtime token and secret snapshot refresh, expiry, invalidation and cleanup.
    status: pending
  - id: tests-docs-handoff
    content: Prove parity and no secret exposure; document consumer migration and tested versions.
    status: pending
  - id: validation-gates
    content: Run ordered quiet quality gates, coverage and package validation.
    status: pending
  - id: final-closure
    content: Reconcile evidence, manual proof and consumer handoff before closure.
    status: pending
---

# 50.0 - Managed identity bootstrap (Python)

## Overview

Add a server-side, pre-client managed-identity bootstrap path for Dataplane while
preserving explicit legacy configuration. This is a security-sensitive SDK feature;
controller authorization and consumer application migration remain separately owned.

## Scope

### In scope

Add an opt-in server-side bootstrap entrypoint for Dataplane.
The application supplies only the non-secret Miso URL, expected Entra audience and
optional managed-identity selection; application context comes exclusively from Miso.
It obtains its approved token and runtime secrets before constructing the SDK or
secret-dependent clients.
No Key Vault SDK, role, secret URL or direct vault read is used by the workload.
Managed-app Miso and Keycloak keep their independent platform startup mechanism.
Workload placement in another RG is supported by registered identity, not RG trust.

### Out of scope

Controller verification, registration or vault implementation; consumer application
code; customer apps; browser secret delivery; arbitrary cross-tenant federation;
normal application/user token transport migration; publishing or deployment.

## Shared contract and dependencies

Authority: [Miso 227.4](../../../aifabrix-miso/.cursor/plans/227.4-installation-keyvault.plan.md).
Parity: [66.0](../../../aifabrix-miso-client/.cursor/plans/66.0-managed-identity-bootstrap.plan.md).

Deployment supplies these non-secret settings:

```dotenv
MISO_AUTH_MODE=azure-managed-identity
MISO_CONTROLLER_URL=https://<installation-miso-host>
MISO_BOOTSTRAP_AUDIENCE=api://<miso-api-application-id>
```

No MISO_CLIENT_ID/MISO_CLIENTID, client secret or application/environment selector is
required. Prefer system-assigned identity; optional AZURE_CLIENT_ID selects an assigned
Azure identity, not a Miso context. Azure mode requires URL/audience with no guessed
public endpoint or developer-credential fallback. Unknown auth mode fails. Absent mode
or `local` uses the existing local `.env` flow through the same helper; it does not
auto-detect Azure.
MISO_AUTH_MODE is independent of the normal request MISO_AUTH_STRATEGY setting.

POST /api/v1/auth/bootstrap with Entra Authorization Bearer and body
`{ "protocolVersion": 1 }` only. Obtain the Entra token with the configured audience's
`/.default` scope through the official Azure Identity managed-identity implementation;
provide an injectable async token-provider seam for tests. No application context,
client ID, tenant override, vault URI or requested secret list is sent. Token audience
selection does not grant authority; Miso verifies its own configured audience and the
registered identity. Do not use SDK decode helpers as identity verification.

The existing success envelope's data carries protocolVersion, issuedAt, server-derived context
(installationId/applicationId/environmentId), clientId, clientToken,
clientTokenExpiresAt, configuration, refreshAfter and expiresAt. Timestamps are UTC
ISO-8601. There is **no clientSecret**. The client ID is returned for SDK compatibility;
normal auth uses only the Miso application token. Miso 227.4 is the wire authority;
share identical sanitized fixtures across SDKs. Endpoint and helper are not yet shipped.
Existing ordinary Bearer-transport plans remain separate; never send the Entra token
as a Miso token to normal APIs. Do not reimplement token signing or identity binding.

## Same application code in local and Azure modes

The user requires the identical initSecrets()/init_secrets() call and runtime API in
local development and Azure. Applications must not branch on deployment mode.
Provider selection belongs inside the SDK:

| MISO_AUTH_MODE | Source | Authentication |
| --- | --- | --- |
| unset or `local` | Existing process environment and normal local `.env` loading | Existing configured Miso client credentials/token flow |
| `azure-managed-identity` | Miso bootstrap broker | Azure managed identity followed by a short-lived Miso token |

For local use, reuse the existing config loader's `.env` behavior and environment
precedence (already-set environment wins over dotenv defaults), including existing
MISO_CLIENTID/MISO_CLIENTSECRET and underscore aliases. MISO_BOOTSTRAP_AUDIENCE is
not required locally. The helper creates the ordinary initialized SDK client and
exposes runtime secrets through the same require/get accessors using the local
configuration source. No Azure dependency, metadata call or bootstrap API request is
needed locally. Missing required local authentication values fail with safe errors.

Both providers expose the same runtime.client, runtime.context, runtime.secrets and
awaitable runtime.close() API. Local context, when needed, is resolved through the
existing authenticated server context flow; locally editable application/environment
values do not become authorization claims. Until context is verified, represent it as
unavailable rather than manufacture a trusted context. APIs remain server-authorized.

The local provider reads a startup snapshot; editing `.env` requires explicit restart/
reinitialization in this version. Ordinary SDK token renewal continues through its
existing local auth path. Local mode has no Entra refresh loop or broker-issued
configuration expiry. The Azure provider retains broker refresh/expiry semantics.
Provider differences must not require application-specific startup/auth branches.

Local `.env` secrets are an explicit development exception to the Azure prohibition
on resolved secrets in environment variables. Local tests must not claim the input
environment or its inherited child environment contains no secrets. Both providers
must prevent new secret persistence, diagnostic leaks and writes of resolved remote
values into the environment; ordinary local dotenv hydration remains supported.

Managed-identity mode never falls back to local after a failure and must not read
legacy credentials even if present. Azure deployment explicitly injects the mode and
removes legacy credentials. Changing mode cannot forge managed identity or registered
context; local credentials, if deliberately supplied, have only their own server-side
permissions. Do not describe a client-side mode setting as a security boundary.

Future Docker secret delivery can implement the same internal provider interface
without changing application code. No Docker provider, token protocol, auto-detection,
secret mount or new accepted mode value is implemented by this plan. Docker may use
the existing local/env provider today; a future broker/mounted-secret provider requires
its own trust, lifecycle and validation contract.

## Deployment-owned selection and fixed v1 policy

Miso processes application.json plus the trusted effective deployment configuration,
confirms the Azure identity and injects MISO_AUTH_MODE, MISO_BOOTSTRAP_AUDIENCE and
MISO_CONTROLLER_URL. The SDK consumes them internally; applications do not inspect
managed identity, choose audiences or register/select context. An Azure workload
without identity fails deployment, never downgrades to local. Local `.env` keeps the
same application code. Caller edits cannot modify Miso's server-side identity binding.

The authoritative numerical/schema/runtime contract is now **227.4, Concrete v1
schema, timing and failure policy**. It replaces previous deferred lifetime/interface
questions. Implement it exactly, with shared fixtures rather than SDK-specific defaults:

- Token 300s, snapshot 900s, refresh jitter 110–120s after issuedAt; 30s maximum clock
  difference and 30s early SDK deadlines, with monotonic protection and no server
  Miso-token expiry grace. issuedAt is now required response data.
- Fresh active-registration check on every Miso authorization, no positive cache;
  failure closed, next check after disable rejects. No claim of downstream password
  revocation or cancellation of operations already authorized.
- SDK denial invalidates all cached token/snapshot paths; token-only expiry blocks
  Miso calls, snapshot expiry blocks secret reads. Changed context/protocol errors
  invalidate. No legacy credential fallback or automatic replay of denied tokens.
- 30s total bootstrap deadline, 5s per HTTP/identity attempt, max 3 HTTP attempts,
  retry jitter [0,1]/[0,2]s, Retry-After max 10s, retries only on network timeout/
  connection failure and 429/502/503/504, one refresh cycle per 30s after failure.
- Decoded response max 1 MiB, 256 config keys, string values max 65536 UTF-8 bytes;
  strict data schema, no duplicate/unknown members, clientSecret or auth/config override.
- Change subscription returns unsubscribe; callbacks are synchronous, ordered and
  names-only. Invalidation reasons: authorization-denied, snapshot-expired,
  protocol-error, closed. Use language naming conventions from 227.4.
- close is awaitable/idempotent with a 5s cleanup deadline, generation guard and
  cancellation. SDK-created resources are owned; injected resources remain caller-owned.

Deliver OpenAPI and canonical Miso tests/fixtures/bootstrap/v1 first, then consume
hash-matched fixture copies in each SDK. Fixture pairs/clock/boundaries are specified
in 227.4. Tests exercise actual normal SDK requests and token caches, not only the
bootstrap helper. Product artifacts are implementation deliverables, not evidence
claimed by this plan-only update.

## Optional Azure packaging

Use the official async azure.identity.aio.ManagedIdentityCredential adapter, imported
lazily, with an optional `azure` extra. Select azure-identity>=1.25.3,<2 and
aiohttp>=3.9,<4 with Python-version markers for Python >=3.9. The base local/env SDK
retains its advertised >=3.8 floor; Azure mode requires >=3.9 and must fail with an
explicit safe compatibility error on 3.8 before network/credential access. Do not
silently install an old identity library or fall back to local. Mirror metadata in
pyproject.toml and setup.py. Dependency resolution chooses a Python-compatible aiohttp;
record resolved versions in package smoke evidence. Test base/no-extra on 3.8 and
Azure extra on 3.9, 3.10, 3.11, 3.12 and 3.13; add newly advertised runtimes only with
matching CI evidence. No dependency installation or runtime support proof is claimed here.

Version metadata checked against [azure-identity 1.25.3](https://pypi.org/project/azure-identity/1.25.3/)
(Python >=3.9); the [official async adapter](https://learn.microsoft.com/en-us/python/api/azure-identity/azure.identity.aio.managedidentitycredential)
provides managed-identity acquisition and close. Validate the optional dependency
resolution on the stated matrix; local imports must succeed without any Azure package.
These package/tests are implementation deliverables. Resolution and runtime evidence are required during implementation validation.

## Architecture alignment and minimal integration

Target public server-only async helper: `init_secrets()`.

```python
runtime = await init_secrets()
miso = runtime.client
database_url = runtime.secrets.require("DATABASE_URL")
```

In Azure mode the helper reads the non-secret settings, bootstraps, constructs token-only
normal SDK configuration and initializes the client. Local mode reuses the existing
`.env` configuration/authentication flow behind the identical runtime API. The application writes no Azure HTTP,
identity parsing or token-renewal code. Call once at the existing async startup boundary;
normal Miso API calls remain unchanged. Runtime exposes client, read-only context,
secrets.require(name), secrets.get(name) and awaitable close(). Secret accessors read
only the current valid in-memory snapshot; require fails for absent/expired keys.

For Mori/Dataplane/Builder API, target one startup call plus replacement of env secret
reads in the existing central config loader. Import-time reads must be deferred.
Do not implement the apparent one-line shortcut of writing resolved secrets into
process.env/os.environ. Never require the existing credential-demanding loadConfig/
load_config before Azure bootstrap. Reuse that loader inside the local provider;
preserve existing explicit constructors/loaders for compatibility.

The SDK owns Entra acquisition, initial bootstrap, Miso token renewal and snapshot
refresh. It pins context for the session; a changed response context fails closed and
requires explicit shutdown/reinitialization. Reject malformed snapshots, missing
required token data and inconsistent timestamps. Refresh before the earliest token or
snapshot deadline, with concurrency coalescing. Authorization denial invalidates the
runtime; transient failures can use only still-valid state. Close cancels timers/tasks
and owned connections. Do not promise secure zeroization of managed-language strings.

For non-Miso application tokens and permitted runtime secrets, atomically replace the secret snapshot and issue a
secret-name-only change notification so the application's existing connection factory
can rebuild affected database/Redis clients. No secrets in notifications, no hidden
readonly-config mutation and no environment/disk/Redis-cache persistence.

Keep Python async I/O and snake_case public functions, with camelCase wire aliases.

## Code reuse scan

Inspect and extend `miso_client/client.py`, `models/config.py`, `utils/internal_http_client.py`, `utils/client_token_manager.py`, `utils/data_masker.py` and `__init__.py`.
Reuse the existing httpx library in a deliberately isolated request path,
following existing token-fetch recursion avoidance. Ordinary HTTP interceptors require
Miso credentials and are therefore unsuitable before bootstrap. Reuse safe structured
error handling, but never retain raw sensitive request/response bodies in exceptions.
No second general-purpose HTTP stack or secret cache service is needed.

### Existing initialization constraints

`miso_client/models/config.py` currently requires client_id and client_secret;
`utils/config_loader.py` also demands credentials, and `client.py` constructs services
in `__init__`. Add a typed token-provider auth alternative with conditional validation
before calling that constructor. Extend ClientTokenManager/InternalHttpClient to use
the bootstrap provider for renewal. Do not satisfy validation with fake/empty secrets.

## Implementation plan

### 1. Define bootstrap-only types and public entrypoint

- [ ] `shared-contract`: Pin controller OpenAPI revision and shared sanitized fixtures;
  define bootstrap-only models and the public entrypoint.

Implement the accepted `init_secrets()` runtime owner described above; do not return
an unmanaged configuration snapshot. Implement 227.4's exact Python interfaces:
`on_secrets_changed(handler)` receives a tuple of key names; `on_invalidated(handler)`
receives the defined reason; both return a synchronous unsubscribe callable. `get`
returns Optional[str]; `require` rejects absent/empty-required or invalid state. Both
reject invalidated state. Listeners run in registration order after atomic update;
catch failures with safe diagnostics and continue. SDK-created resources are owned;
injected resources stay caller-owned. The runtime schedules refresh and disables further
SDK token access on denial/close; consumer connection factories stop/rebuild external
clients on invalidation/rotation. Previously returned strings cannot be revoked by
clearing an SDK reference. Specify permitted configuration keys and explicit mapping into
MisoClientConfig; broker configuration cannot override the pinned controller origin,
auth strategy or bootstrap identity selection.

Proposed file seams: `models/bootstrap.py`, `utils/bootstrap.py`,
`utils/bootstrap_identity.py`, and `utils/bootstrap_snapshot.py`; expose documented
symbols through `miso_client/__init__.py` without importing Azure eagerly.

Additional implementation best-practices to apply:

- Keep bootstrap input separate from required normal application tokens and permitted runtime secrets; reject
  incompatible protocol, changed server-derived context and expired response snapshots.
- Centralize the endpoint constant and wire fixtures; keep public exports server-only.

### 2. Implement local provider, mode selection and packaging

- [ ] `local-provider-packaging`: Select mode once from process settings before
  dotenv hydration; unknown mode fails. Implement the local runtime with the existing
  loader and startup secret snapshot; dotenv cannot switch the selected provider.
  Reuse ordinary local token renewal, expose unverified context as unavailable, and
  provide the same accessors/subscriptions/close API without Azure imports or calls.

Add the selected `azure` extra to pyproject.toml and setup.py. Lazy-load the official
async ManagedIdentityCredential adapter only for Azure mode; reject unsupported Python
or a missing extra with safe errors before network access. Preserve local >=3.8 and
prove resolved Azure-extra installs on the matrix in Optional Azure packaging.
Add `tests/unit/test_bootstrap_local.py` for both aliases, dotenv precedence, poisoned
mode settings, snapshot restart behavior and common runtime cleanup.

### 3. Acquire identity and request credentials

- [ ] `pre-client-bootstrap`: Implement the isolated async transport and optional
  managed-identity adapter after canonical schema/fixtures are available.

Use the audience-to-`/.default` mapping and identity selection defined by 227.4. Validate URL scheme, origin and absence of userinfo/query/fragment before
requesting a token. Stream a bounded response before parsing; avoid retries extending
past the total deadline. Reject malformed/mismatched 200 responses and enforce the
contract's UTC timestamp ordering. Treat 422 as terminal alongside 401/403.
Distinguish TLS validation failure from retryable connection failure. Retry-After >10s
ends the operation; do not clamp it and retry early. Reject duplicate JSON members
before Pydantic parsing and enforce decoded size before materializing the full body.

Additional implementation best-practices to apply:

- Use an async managed-identity token provider for the configured bootstrap audience;
  send the token only to the configured HTTPS Miso endpoint, with redirects disabled.
- Bypass normal token interceptors and authenticated logging to avoid recursion;
  suppress body/header capture before errors or telemetry can serialize them.
- Bound timeout, response size and retries; retry only transient failures with jitter
  and Retry-After handling, never loop on 401/403 or unsupported protocol.

### 4. Integrate token-only normal SDK authentication

- [ ] `token-provider-integration`: Extend `models/config.py`,
  `utils/client_token_manager.py` and `utils/internal_http_client.py` with a typed
  async application-token provider alternative. Preserve legacy credential auth;
  reject ambiguous configuration rather than supplying a fake client secret.

All normal token acquisition, cache checks and 401 invalidation must consult the
runtime provider in Azure mode. Never call the legacy credential exchange there.
Provider tokens use the validated `clientTokenExpiresAt` deadline without the existing
1800-second malformed-expiry fallback. A copied cache must not bypass runtime denial,
close or context invalidation. Keep user-token handling and the transport selected by
49.0 independent; do not introduce a new Bearer/header migration in this plan.

Prove a real SDK request and renewal after initialization, not just successful
`MisoClient.initialize()` (currently it connects Redis without fetching a token).
Add provider success/renewal/denial/expiry/401 cases to token-manager and HTTP tests.

### 5. Manage the in-memory snapshot

- [ ] `snapshot-lifecycle`: Implement snapshot refresh, invalidation and disposal
  according to the resolved public lifecycle API.

Implement 227.4 single-flight task cancellation, injected wall/monotonic clocks,
30-second failure cooldown and a generation guard so late responses cannot repopulate
a disposed or denied snapshot. Enforce the 5-second close/failed-init cleanup deadline. Test exact expiry and clock-skew boundaries with shared fixtures.

Azure mode receives no Miso client secret. Keep its application token and runtime
secret values in non-serializable/private provider state, outside normal config dumps;
the config carries the provider reference with safe repr/serialization behavior.
Preserve the legacy constructor and local credential path. Test runtime/provider/config
repr and validation/traceback output with sentinels for both modes; do not claim
arbitrary caller serialization or already-returned strings are secret-safe. Suppress
Pydantic input values and sensitive exception causes/contexts at the error boundary.

Additional implementation best-practices to apply:

- Refresh via Entra at the specified issuedAt + 110–120s target and share refresh work.
  Explicit denial invalidates the snapshot; transient failure may reuse only an
  unexpired snapshot. Cold-start failure blocks initialization.
- In Azure mode never write process environment, dotenv, disk/Redis caches, subprocess credentials
  or serialized logs; release references on disposal without claiming guaranteed
  secure erasure in a managed runtime.
- Atomically replace the internal secret snapshot; emit changed key names only and
  document external client rebuilds. Miso-token renewal stays transparent through the
  provider; do not replace runtime.client on every token refresh.

### 6. Prove parity and deliver consumer guidance

- [ ] `tests-docs-handoff`: Add `tests/unit/test_bootstrap.py`,
  `tests/unit/test_bootstrap_identity.py`, `tests/unit/test_bootstrap_snapshot.py`
  and sanitized shared fixtures under `tests/fixtures/bootstrap/v1/`.
  Update `README.md`, `docs/configuration.md` and a new
  `docs/managed-identity-bootstrap.md` with startup, refresh and shutdown examples.

Mock provider, network, time and external client construction in automated tests;
keep real identity proof in the separately authorized manual suite. Azure fresh-process
proof removes both compact and underscore credential aliases and prevents dotenv
loading. Local proof uses normal dotenv loading and validates input precedence. In Azure mode,
assert constructors as well as initialize calls occur after bootstrap.

Additional implementation best-practices to apply:

- In Azure mode, test a fresh process without Miso credentials and assert bootstrap
  completes before normal token fetch, Redis, database and SDK initialization.
- Use synthetic sentinel credentials in success and failure capture tests; do not
  forward raw transport error objects containing headers or response bodies.
- Document compatibility/version gates and the consumer's startup and restart hooks;
  a package upgrade alone does not migrate import-time application configuration.

## Expected Automated Tests

- Same unmodified application init/secret-access fixture consumes Miso-generated settings
  for Azure and normal local .env; application code performs no provider detection.
- All authoritative 227.4 schema/deadline/retry/revocation/cleanup fixtures pass, including
  fresh protected requests after disable and suppression of late/cached tokens.
  Pin the canonical source hash. Include skew 30/31s, token 269/270s, snapshot
  869/870s, Retry-After 10/11s, attempts/deadlines/size and close-during-refresh pairs.
  Controller fixtures prove the configuration allowlist; SDK cases prove reserved-key,
  syntax and type rejection without inventing a caller-supplied authorization list.

- Run the same minimal consuming-application fixture in local and Azure modes with
  identical initialization/secret-access code; only deployment settings differ.
- Local `.env` and environment precedence, existing credential aliases, optional
  audience, missing local auth, require/get and close work without Azure imports/calls.
- Local file edits take effect on restart; existing token refresh remains functional.
- Azure mode ignores local credentials and does not downgrade on failed bootstrap;
  an unknown mode fails. Neither mode turns caller context into authorization.
- Local supplied secrets never reach diagnostics or new output files; Azure sentinel
  tests additionally prove remote secrets never enter process/child environments.

- New helper initializes an actual normal SDK client from only mode/URL/audience;
  no preexisting client ID/secret and no later hidden credential-exchange dependency.
- MISO_AUTH_MODE selection, required URL/audience, unknown mode and explicit legacy
  behavior; managed-identity mode cannot fall back to developer/env credentials.
- Request body has only protocolVersion; caller config cannot override server context.
  Changing audience/client ID/environment settings cannot elevate authorization.
- No clientSecret response accepted as a substitute for required clientToken; renewal
  uses Entra bootstrap and normal token transport remains compatible with existing APIs.
- Secret require/get, expired access, atomic refresh, secret-name-only change events,
  pinned context and close cleanup work in a minimal consuming-application fixture.

- Successful v1 bootstrap creates valid normal config without preexisting credentials.
- No Key Vault requests; managed identity token only reaches the pinned broker origin.
- `[EDGE]` Bad/expired/mismatched response, 401/403, timeout, 429, 503, redirect and
  unsupported protocol fail safely; no silent legacy or anonymous fallback.
- Concurrent refresh, expiry, explicit denial, cancellation/disposal and client rebuild
  behavior match the other SDK's fixtures and timestamp semantics.
- Azure-resolved credentials and tokens absent from environment, subprocess environment, disk,
  Redis, HTTP logging, exceptions, repr/serialization diagnostics and audit queues.
- Legacy explicit config still works; regular application/user auth transport unchanged.
- Async provider and HTTP clients close correctly under cancellation.

## Manual testing

After controller delivery, run a reusable synthetic proof under tests/manual against
dev-reload Miso, then authorized managed-identity proof for the owning workload.
Record only status, version and safe correlation data in .temp/plan-validation/50.0/.
Include no preexisting Miso credentials, a cross-RG registered workload, a denied
binding, Miso outage and forbidden direct Key Vault read. Live provisioning and actual
consumer migration belong to the owning application, not SDK plan-only work.
Plan close-out requires a linked owning-application proof with tested versions and
safe evidence for the Azure scenarios; lack of that environment is an explicit
remaining gate, never a mock-based claim of production readiness.

## File Size Reduction Candidates

Keep bootstrap transport and response validation in focused helpers; do not grow the
normal HTTP client with Azure startup logic. Follow repo 500-line source and 20–30-line
method guidance with documented existing exceptions.

## Rules and Standards

Applicable [project rules](../rules/project-rules.mdc):

- [Architecture Patterns](../rules/project-rules.mdc#architecture-patterns): isolated
  pre-client httpx path, ordinary SDK transport unchanged.
- [Code Style](../rules/project-rules.mdc#code-style) and
  [Documentation](../rules/project-rules.mdc#documentation): typed async signatures,
  snake_case internals, camelCase wire aliases, Google-style public docstrings.
- [Security Guidelines](../rules/project-rules.mdc#security-guidelines): fail-closed
  bootstrap, safe structured errors and no credential-bearing diagnostic capture.
- [Testing Conventions](../rules/project-rules.mdc#testing-conventions): mocked I/O,
  success/failure coverage and >=80% branch coverage for new code.
- [Code Size Guidelines](../rules/project-rules.mdc#code-size-guidelines): <=500-line
  source files and <=20–30-line methods; only documented repository exceptions.
- [Dependencies](../rules/project-rules.mdc#dependencies) and
  [File Organization](../rules/project-rules.mdc#file-organization): optional Azure
  imports, existing httpx reuse and verified wheel/export contents.

Read these rules before implementation. Bootstrap is a narrowly
scoped exception to ordinary Miso client-token authentication, authorized by 227.4;
normal API header behavior stays unchanged. Fail closed through the established
transport error boundary instead of returning an empty successful credential config.
No secret-bearing diagnostics, browser exposure, Key Vault access or publish actions.

## Before Development

- [ ] `before-development`: Read applicable rules, resolve the prerequisites below,
  review reuse seams, and capture baseline quality results without absorbing unrelated
  working-tree changes.

- Validate controller OpenAPI and identity binding, audience provisioning and lifetime
  semantics; use identical response and error fixtures in both SDKs.
- Implement the fixed 227.4 notification/cleanup and retry/size/clock policy, and the
  optional Azure extra/Python matrix selected in this plan. Mirror metadata in
  pyproject.toml/setup.py; preserve base >=3.8 and enforce Azure >=3.9 explicitly.
- Select mode from process settings before dotenv side effects. Explicit Azure mode
  never loads dotenv; local/unset mode retains local hydration. Freeze the selection:
  a dotenv-only mode value cannot switch an already selected local provider.
- Validate refresh/deadlines using shared fixtures. A response whose refreshAfter has
  already passed is invalid at receipt; never spin or extend deadlines through fallback.
- Record tested minimum controller/SDK/consumer versions before rollout. No existing
  release is assumed to implement the proposed bootstrap protocol.
- Validate and approve this plan before product edits; capture baseline quality gates.

## Definition of Done

The same init helper and runtime API must pass local `.env` and Azure consumer
fixtures without application mode branches. Docker provider delivery remains future work.

1. The same async init helper creates the initialized client and secret accessor in
   both modes. Azure needs no preexisting Miso credentials or application-owned token
   renewal; local mode preserves its explicit credentials and dotenv behavior.
2. No workload vault reads or writes of Azure-resolved values to environment/disk
   occur. Local dotenv hydration is the documented development exception.
3. Refresh, denial, expiry, safe diagnostics and parity tests pass.
4. Public API documentation includes startup order, Azure configuration, platform
   exception, client rebuilds, outage behavior and compatibility without secret examples.
5. Run FORMAT → LINT → BASEDPYRIGHT → TYPE CHECK → TEST in order, with no skipped
   gates: `make format-silent`, `make lint-silent`, `make basedpyright-silent`,
   `make type-check-silent`, `make test-silent` (or `make validate-silent`).
   Require zero errors/warnings; logs live under `.temp/validation/`. Use corresponding
   non-silent targets only if wrappers are unavailable. Require >=80% branch coverage
   on new code, then `make build` and installed-wheel import checks with and without
   the Azure extra. All functions are typed; public methods have Google-style
   docstrings. Meet the file/method limits documented above.
6. Consumer handoff records Dataplane initialization work,
   released versions and live proof requirements; no automatic fallback reintroduces secrets.

## Validation and closure tasks

- [ ] `validation-gates`: Run the DoD sequence, focused sentinel/parity/lifecycle
  cases, coverage and wheel checks; attach exact commands and results.
- [ ] `final-closure`: Reconcile todos with evidence, attach authorized manual proof
  and consumer handoff, and rerun lifecycle review. No completion claim while required
  contract, security or live-proof gates remain unresolved.

## Plan Validation Report

**Date**: 2026-09-19 (today is 2026-09-19)
**Plan**: `.cursor/plans/50.0-managed-identity-bootstrap.plan.md`
**Status**: VALIDATED — design and implementation plan; delivery remains pending.

### Purpose and scope

Server-side SDK feature providing the same `init_secrets()` runtime in local and
Azure modes. The SDK owns managed identity, token-only auth, refresh, safe secret
access and cleanup; Miso owns identity binding and authorization; consumers own
external connections. No publishing, provisioning or product implementation occurs
in this validation.

### Evidence and resolved findings

Reviewed the updated 227.4 Concrete v1 schema, timing and failure policy, runtime
interfaces and delivery order against 66.0 and this plan. The previous timing,
revocation, retry, schema and ownership design questions are resolved: fixed token
and snapshot deadlines, early expiry, strict response bounds, finite retries,
names-only ordered callbacks and bounded cleanup now have explicit acceptance cases.

Rechecked Python configuration requirements, token-manager credential exchange and
expiry fallback, client initialization, package metadata and Make targets. Tasks
explicitly address these existing constraints without faking a client secret or
mistaking Redis initialization for usable authentication. Rechecked Azure Identity
1.25.3 package metadata and Microsoft's async adapter reference, linked in Optional
Azure packaging: dependency resolution and install/import proof remain implementation
checks, not asserted results.

### Rule compliance and changes applied

Applicable architecture, security, testing, code style/size, documentation, dependencies
and organization rules are linked above. Mandatory quiet FORMAT → LINT → BASEDPYRIGHT
→ TYPE CHECK → TEST gates, >=80% new-code branch coverage, build and installed-wheel
checks are documented. Added the local-provider/packaging task and its focused tests;
moved the selected package policy into the operative plan. Replaced obsolete requests
to decide already-fixed interfaces with implementation requirements and exact boundary
cases. Clarified Retry-After, TLS, duplicate JSON and fixture ownership behavior.

All nine frontmatter todos map to pending body checkboxes. Structural checks verify
frontmatter, task mapping, local links and a single report; `git diff --check` passes.
No product tests, package installs, builds, live Azure calls or consumer migrations
were performed or marked complete.

### Dependency and release gates

- Controller OpenAPI and canonical `tests/fixtures/bootstrap/v1/` are the first
  controller implementation deliverable. Consume hash-matched copies before SDK wire
  implementation. Their absence today does not contradict the accepted design.
- Controller 227.4 and sibling 66.0 keep their own validation/execution status; this
  report validates Python 50.0 only. Coordinate contract changes across owners.
- Resolve/install the selected Azure extra on the stated Python matrix; test the base
  package without Azure. A compatibility failure must be repaired before closure.
- Capture controller/SDK/consumer version evidence, authorized cross-RG bootstrap and
  forbidden direct vault access before rollout. These remain required delivery proof.

### Application Development Lifecycle Coverage

| Lifecycle area | Status | Phase | Evidence / remaining proof |
| --- | --- | --- | --- |
| Product intent | covered | plan | Same application API in local/Azure; no workload vault access |
| Architecture fit | covered | plan | Runtime/provider ownership and controller/consumer boundaries |
| Contract/schema | planned | plan+close | Fixed 227.4 v1 policy; task 1 pins OpenAPI and canonical fixture hash |
| Security/governance | planned | plan+close | Fixed revocation/deadline policy; tasks 3–6 prove denial, isolation and safe diagnostics |
| Implementation | planned | close | Tasks 1–5 cover models, local provider, Azure adapter, normal auth and lifecycle |
| Validation | planned | close | Task 6, boundary fixtures, ordered DoD gates and package matrix |
| Observability/explainability | planned | close | Sentinel tests, allowlisted diagnostics and value-free callbacks |
| Documentation/rules | planned | close | README/configuration/managed-identity guide and startup/rotation examples |
| Promotion readiness | planned | close | Required owning-workload proof and versioned consumer handoff |

Lifecycle decision: validated for plan readiness. No unresolved design blocker remains
in Python 50.0; implementation tasks and dependency/release gates above remain pending.
Plan validation is not implementation completion or evidence that the endpoint ships.

## Implementation approval and prerequisite check

User approved this validated plan in the current task. Approval covers Python 50.0
implementation in its documented dependency order; it does not mark any task complete.

The prerequisite check found no canonical controller
`packages/miso-controller/tests/fixtures/bootstrap/v1/` directory and no
`/api/v1/auth/bootstrap` definition in controller OpenAPI. Controller 227.4 owns
those first deliverables. SDK wire implementation must wait for them so fixture
expectations are copied and hash-matched rather than independently invented.

Python implementation approval remains valid when these artifacts arrive; no repeat
approval is required for the already approved scope. No controller files changed.

Baseline: `make validate-silent` passed format, lint, basedpyright, type-check and
tests. Logs: `.temp/validation/{01-format,02-lint,03a-basedpyright,03-type-check,04-test}`.
The working tree has no product-code changes. All implementation todos remain pending
until the prerequisite artifact handoff; approval status is distinct from completion.
