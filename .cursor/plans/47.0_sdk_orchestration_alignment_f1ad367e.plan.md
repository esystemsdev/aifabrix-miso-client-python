---
name: 47.0 sdk orchestration alignment
overview: "Create a miso-client-python follow-up plan for dataplane plan 442.0 handoff pack C: aligned release + docs/changelog terminology sync, while keeping Python runtime behavior unchanged unless an explicitly verified mismatch is approved."
todos:
  - id: verify-rules-before-development
    content: Read and lock applicable sections from `.cursor/rules/project-rules.mdc` before making release/docs alignment changes.
    status: pending
  - id: freeze-442-handoff-c-constraints
    content: Freeze fixed decisions from 442.0 (handoff pack C), including Python runtime no-change scope and browser-orchestration boundary.
    status: pending
  - id: verify-python-runtime-scope-boundary
    content: Verify current Python auth/session runtime surfaces and document evidence that browser activity orchestration is not part of Python runtime scope for this rollout line.
    status: pending
  - id: sync-cross-sdk-terminology-docs
    content: Update Python SDK docs to align migration terminology with TS SDK final behavior and explicitly mark browser activity orchestration as TypeScript/browser-only.
    status: pending
  - id: prepare-aligned-python-release-entry
    content: Prepare aligned Python SDK release/changelog entry for this rollout line, including explicit scope boundary and cross-SDK terminology synchronization.
    status: pending
  - id: sync-version-files-for-release
    content: Keep versioning files consistent for aligned release entry (`pyproject.toml`, `setup.py`, `miso_client/__init__.py`, optional `.bumpversion.cfg`).
    status: pending
  - id: validate-no-new-runtime-knobs
    content: Confirm no new Python runtime mandatory knobs are introduced and add explicit compatibility note in docs/changelog.
    status: pending
  - id: run-validation-gates
    content: Run repository validation gates in required order using quiet commands first (`format -> lint -> type-check -> test -> integration`).
    status: pending
  - id: final-dod-sync
    content: Synchronize frontmatter todos, checklist items, and completion evidence; do not close if any stop condition is triggered.
    status: pending
isProject: false
---

# Plan 47.0 — SDK Orchestration Alignment (miso-client-python)

## Goal
Deliver the `miso-client-python` follow-up for dataplane plan 442.0 handoff pack C: publish an aligned Python SDK release and synchronize documentation terminology with the TypeScript SDK rollout, while keeping Python runtime behavior unchanged for browser activity orchestration in this line.

Source: [/workspace/aifabrix-dataplane/.cursor/plans/442.0_sdk_orchestration_core_0f148c97.plan.md](/workspace/aifabrix-dataplane/.cursor/plans/442.0_sdk_orchestration_core_0f148c97.plan.md), section `Handoff Pack C - miso-client-python agent plan input`.

## Rules and Standards
This plan must comply with [project rules](/workspace/aifabrix-miso-client-python/.cursor/rules/project-rules.mdc), with focus on:

- **[Architecture Patterns](/workspace/aifabrix-miso-client-python/.cursor/rules/project-rules.mdc#architecture-patterns)** - preserve request-driven auth/session behavior and token boundary semantics.
- **[Code Style](/workspace/aifabrix-miso-client-python/.cursor/rules/project-rules.mdc#code-style)** - keep Python conventions, type hints, and safe error-handling behavior if code is touched.
- **[Testing Conventions](/workspace/aifabrix-miso-client-python/.cursor/rules/project-rules.mdc#testing-conventions)** - ensure touched behavior/docs have truthful test coverage expectations.
- **[Security Guidelines](/workspace/aifabrix-miso-client-python/.cursor/rules/project-rules.mdc#security-guidelines)** - no credential leakage, no unsafe token/log guidance in docs.
- **[Code Size Guidelines](/workspace/aifabrix-miso-client-python/.cursor/rules/project-rules.mdc#code-size-guidelines)** - keep touched source within limits if runtime code changes are explicitly approved.
- **[Documentation](/workspace/aifabrix-miso-client-python/.cursor/rules/project-rules.mdc#documentation)** - maintain clear, Google-style, consumer-facing migration guidance consistency.

### Key Requirements
- Runtime behavior changes are disallowed by default in this plan line; require verified mismatch evidence and explicit approval.
- Browser activity orchestration ownership must stay explicitly documented as TypeScript/browser-only scope.
- Cross-SDK migration wording must remain consistent across README/docs/changelog and release notes.
- Validation order must remain strict and quiet-first.

## Scope
### In scope
- Release/docs alignment for Python SDK as required by 442.0 handoff pack C.
- Explicit scope boundary documentation: browser activity listener orchestration is TypeScript/browser SDK scope, not Python runtime scope for this rollout line.
- Cross-SDK migration terminology synchronization across Python SDK docs/changelog.
- Validation evidence for touched Python/docs paths.

### Out of scope
- Implementing browser activity orchestration runtime behavior in Python SDK.
- Introducing new Python runtime mandatory knobs/config fields.
- Unapproved Python runtime behavior changes not backed by verified mismatch and explicit approval.

## Fixed Decisions (from 442.0)
- Python SDK runtime behavior stays unchanged for browser activity orchestration in this rollout line.
- Deliverable is release/docs alignment and terminology synchronization.
- Cross-SDK guidance must be consistent with TypeScript SDK final behavior.
- No new Python runtime mandatory knobs are introduced.

## Primary Files
- [/workspace/aifabrix-miso-client-python/README.md](/workspace/aifabrix-miso-client-python/README.md)
- [/workspace/aifabrix-miso-client-python/docs/backend-client-token.md](/workspace/aifabrix-miso-client-python/docs/backend-client-token.md)
- [/workspace/aifabrix-miso-client-python/docs/integration-test-requirements.md](/workspace/aifabrix-miso-client-python/docs/integration-test-requirements.md)
- [/workspace/aifabrix-miso-client-python/CHANGELOG.md](/workspace/aifabrix-miso-client-python/CHANGELOG.md)
- [/workspace/aifabrix-miso-client-python/pyproject.toml](/workspace/aifabrix-miso-client-python/pyproject.toml)
- [/workspace/aifabrix-miso-client-python/setup.py](/workspace/aifabrix-miso-client-python/setup.py)
- [/workspace/aifabrix-miso-client-python/miso_client/__init__.py](/workspace/aifabrix-miso-client-python/miso_client/__init__.py)

## Before Development
- [ ] Re-read 442.0 `Handoff Pack C` and `Fixed decisions` sections and freeze them as immutable constraints.
- [ ] Confirm runtime scope boundary: Python SDK does not implement browser activity orchestration in this rollout line.
- [ ] Confirm release/docs outputs expected by 442.0 are explicitly listed in this plan.
- [ ] Confirm versioning targets and release-note format for aligned Python release entry.
- [ ] Confirm validation command order and quiet-first troubleshooting flow.

## Implementation Steps
1. Validate fixed decisions from 442.0 handoff pack C and lock them as non-negotiable constraints for this plan.
2. Verify current Python runtime surfaces (`auth_api`, token/refresh behavior) and collect evidence that browser activity orchestration is not a Python runtime feature in this plan line.
3. Update docs to synchronize cross-SDK migration terminology and explicitly state Python runtime scope boundary.
4. Add/update changelog and release note content for aligned Python SDK release in this rollout line.
5. Synchronize release version files (`pyproject.toml`, `setup.py`, `miso_client/__init__.py`, and `.bumpversion.cfg` if used) for aligned release.
6. Ensure no new Python runtime mandatory knobs are introduced; include explicit compatibility note.
7. Run validation gates and resolve failures before closure.
8. Sync checklist/todos and finalize only after all stop conditions remain clear.

## Stop Conditions
- If proposed changes introduce Python runtime browser activity orchestration behavior, stop and request explicit approval.
- If docs do not explicitly preserve the TS-browser vs Python-runtime boundary, do not mark complete.
- If aligned release/changelog deliverables are missing, do not mark complete.
- If version values are inconsistent across release-version files, do not mark complete.

## Expected Automated Tests
- Unit/contract: existing auth/session tests still pass, confirming no unintended runtime behavior drift.
- Integration: existing API integration suite remains green for request-driven flows and token boundary behavior.
- Regression: no new runtime knobs or browser-orchestration behavior added in Python runtime surfaces.
- Documentation contract check: updated docs/changelog explicitly preserve TS-browser vs Python-runtime boundary wording.

## Validation Commands
Run from `/workspace/aifabrix-miso-client-python` in required order:
1. `make format-silent` (backup: `make format`)
2. `make lint-silent` (backup: `make lint`)
3. `make type-check-silent` (backup: `make type-check`)
4. `make test-silent` (backup: `make test`)
5. `make test-integration-silent` (backup: `make test-integration`)

If a quiet step fails, inspect `.temp/validation/` logs first, then rerun failing command with full output if needed.

## Definition of Done
- Handoff pack C requirements from 442.0 are fully mapped and honored.
- Python runtime behavior remains unchanged for browser activity orchestration scope.
- Docs/changelog explicitly state browser orchestration boundary and synchronized cross-SDK terminology.
- Aligned Python SDK release entry/versioning artifacts are prepared for this rollout line.
- Version values are consistent across `pyproject.toml`, `setup.py`, `miso_client/__init__.py`, and `.bumpversion.cfg` (if used in repo workflow).
- No new Python runtime mandatory knobs are introduced.
- Validation gates pass in mandatory order.
- Frontmatter todos and markdown checklist are synchronized with evidence-backed completion.